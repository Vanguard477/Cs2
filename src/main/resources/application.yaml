spring:
  application:
    name: cs2
  datasource:
    url: jdbc:h2:mem:cs2;NON_KEYWORDS=user
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/master.xml
  h2:
    console:
      enabled: true

